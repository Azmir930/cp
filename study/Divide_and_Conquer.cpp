#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int Find_Maximum_Subarray(ll A, ll low , ll high){
    if(high == low)return(low, high, A[low]
    );
    else{ 
        mid= 
    }
}

int main(){
     vector<ll> A = {13, -3, -25, 20, -3, -16, -23,
                    18, 20, -7, 12, -5, -22, 15, -4, 7};
    ll low =0;
    ll high = A.size()-1;
    auto[start, end, max, max_sum]= Find       
}